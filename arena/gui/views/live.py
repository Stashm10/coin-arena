import flet as ft


def build_live(page: ft.Page, on_back) -> ft.View:
    """Placeholder for Task 14's real QME live view. Only exists so app.py's
    routing has something to import and render for the /live route."""
    return ft.View(route="/live")
